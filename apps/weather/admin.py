"""
Admin configuration for the Weather App.

No models are registered yet. When you add models (e.g. SearchHistory),
register them here so they appear in the Django admin panel.
"""

from django.contrib import admin

# Register your models here.
